name: First Action

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "LS"
      - name: multiline stings
        run: |
          node -v
          npm -v
